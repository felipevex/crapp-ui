package crapp.ui.style.types;

enum abstract CrappUIStyleDefaultTagType(String) from String to String {
    
    var BUTTON;
    var BUTTON_ICON;
    var SELECT_INPUT;
    var TEXT_INPUT;
    var TEXT;
    var IMAGE;
    var SURFACE;
    var LINE;
    var MODAL;
    var DIALOG;
    var CONTEXT_MENU;
}